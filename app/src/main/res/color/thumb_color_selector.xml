<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Цвет, когда switch включен (checked) -->
    <item android:color="?attr/switchThumbColorChecked" android:state_checked="true"/>
    <!-- Цвет, когда switch выключен (unchecked) -->
    <item android:color="?attr/switchThumbColorUnchecked" android:state_checked="false"/>
</selector>